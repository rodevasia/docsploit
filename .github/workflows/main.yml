name: Waka Readme

on:
  workflow_dispatch:
  schedule:
    # Runs at 12am UTC
    - cron: "0 0 * * *"

jobs:
  update-readme:
    name: wakalogs
    runs-on: ubuntu-latest
    steps:
      - uses: docsploit/docsploit@main
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
